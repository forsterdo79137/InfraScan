{
  "name": "@infrascan/worker",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo \"No build step for Python worker\"",
    "lint": "echo \"Add Ruff/Flake8 if desired\"",
    "typecheck": "echo \"Add mypy if desired\"",
    "clean": "rm -rf __pycache__ .mypy_cache"
  }
}


